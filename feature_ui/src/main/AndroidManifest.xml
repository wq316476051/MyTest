<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wang.mytest.feature.ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--animation-->
        <activity android:name=".animation.FragmentAnimationActivity" />

        <!--views-->
        <activity android:name=".view.DeleteDialogActivity" />
        <activity android:name=".view.DrawActivity" />
        <activity android:name=".view.DrawRecordActivity" />
        <activity android:name=".view.ViewActivity" />
        <activity android:name=".fragment.FragmentAlertDialogSupport" />
    </application>

</manifest>